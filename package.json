{
  "name": "roast-my-startup",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "npm run build && wrangler pages dev dist",
    "deploy": "npm run build && wrangler pages deploy dist --project-name=roast-my-startup --branch=main"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.1.0",
    "@sentry/node": "^10.66.0",
    "@sentry/react": "^10.66.0",
    "@vercel/analytics": "^2.0.1",
    "@vitejs/plugin-react": "latest",
    "botid": "^1.5.11",
    "html-to-image": "^1.11.13",
    "posthog-js": "^1.404.1",
    "react": "latest",
    "react-dom": "latest",
    "vite": "latest"
  },
  "devDependencies": {
    "wrangler": "^4.112.0"
  }
}
